Use the notebook-enhancer agent (Opus, high reasoning) to run a focused polish round on
the L2B2 notebook ONLY, editing IN PLACE.

SCOPE LOCK: modify only jax-examples/notebooks/01_domain_shift_toy.ipynb and
jax-examples/src_01_domain_shift_toy.py, in place. Do NOT produce _v2 or any other new
version, and do not create, modify or delete any other file in the repository.

Follow the working procedure in the brief: first resync src_01_domain_shift_toy.py from
the notebook with jupytext (the notebook is source of truth and may contain hand edits),
then apply all changes to that .py, regenerate the .ipynb from it, and execute headless in
the WinterSchool kernel until green, leaving both files consistent.

Read dev/agents/work/enhance/L2B2_polish3_brief.md (authoritative for this round) and the
current notebook.

Think hard before writing. Apply the brief: (R1) rebuild the data generator as a
parametric shape family — keep three classes and the current imbalance, but replace the
blob-like supports with non-convex, interleaved, curved geometry (a spiral/crescent arm, an
interleaving arm or annulus, and a multi-modal rare class placed in the ambiguous regions),
all defined by generative parameters so source and target come from the same function;
(R2) much larger samples (~20k train, ~10k validation, split sizes printed) and
higher-resolution probability/decision maps with publication-clean styling, with model
capacity tuned so contours are rich but not perfect; (R3) produce the target by perturbing
the GENERATIVE PARAMETERS heterogeneously per class — deforming shapes rather than
translating clouds — including a prior shift, named against L2B1's taxonomy, with an
unmistakable source-vs-target closing figure; (R4) visualization polish throughout with
consistent class colours and axis limits, keeping all existing evaluation machinery working.

Keep the seven-block structure and pt-BR narrative. Save no figures. Keep runtime
reasonable on CPU. Report ENHANCED GREEN with the realized class proportions, sample sizes,
and total runtime.
