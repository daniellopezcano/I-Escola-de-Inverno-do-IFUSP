Use the notebook-enhancer agent to fully rebuild the L3B2 notebook.

First, resync the mirror: regenerate jax-examples/src_02_contrastive_embeddings.py from
jax-examples/notebooks/02_contrastive_embeddings.ipynb with jupytext, per the
source-of-truth policy in CLAUDE.md.

Then read dev/agents/work/enhance/L3B2_brief.md (authoritative), the CURRENT
course-materials/L3B1.md (the theory block this notebook must mirror — derive the section
mapping from the file itself, since it was recently renumbered), and the polished
jax-examples/notebooks/00_caixa_de_ferramentas.ipynb and 01_domain_shift_toy.ipynb as
style exemplars (Equinox/Optax, inline helpers, short cells, pt-BR, visual-first).

Think hard about the four-part narrative and its runtime budget before writing, then build:
(1) MNIST raw pixel space is hard to interpret — same-digit images far apart in pixel
distance, high dimensionality made concrete; (2) a small Equinox CNN encoder with a 2D
BOTTLENECK (not a U-Net, no skip connections around the bottleneck) trained as a supervised
classifier, with the emergent 2D latent-space scatter as the key figure; (3) MNIST
augmentations implemented and VISUALIZED, including an explicit positive-pair vs
negative-pairs figure, framed as declarations of invariance; (4) self-supervised training
with an explicit, readable InfoNCE loss (normalized embeddings, cosine similarity,
temperature) whose code maps visibly onto L3B1's equation, recovering digit structure in
the 2D embedding WITHOUT labels, compared side by side with part 2, plus a linear-probe
sanity check.

Every conceptual point must produce a figure the instructor can lift into slides. Download
MNIST at runtime, save no figures, write nothing to a committed path. Keep runtime modest
on CPU (subsample MNIST, small nets, few epochs).

Produce jax-examples/notebooks/02_contrastive_embeddings_v2.ipynb and leave the current
notebook untouched. Verify it runs clean end-to-end in the WinterSchool kernel and report
ENHANCED GREEN with a summary of design decisions and actual runtime.
