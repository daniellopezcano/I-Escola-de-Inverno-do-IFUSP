Use the notebook-enhancer agent to run a POLISH round on the L2B2 notebook.

First, resync the mirror: regenerate jax-examples/src_01_domain_shift_toy.py from
jax-examples/notebooks/01_domain_shift_toy.ipynb with jupytext, per the source-of-truth
policy in CLAUDE.md.

Then read dev/agents/work/enhance/L2B2_polish_brief.md (authoritative for this round),
the canonical jax-examples/notebooks/01_domain_shift_toy.ipynb (the baseline to polish),
and jax-examples/notebooks/00_caixa_de_ferramentas.ipynb (L1B2 — the style exemplar).

Think hard before writing. Keep the seven-block structure intact and apply the brief:
(R1) a genuinely non-trivial source distribution — non-convex, interleaved, one
multi-modal class, real overlap, imbalanced, many more points, with model capacity tuned
to match; (R2) the upgraded confusion-matrix plot with counts, row percentages and
diagonal TPR/PPV/F1 on a diverging colormap; (R3) a class-dependent, heterogeneous target
shift including a prior shift, named against L2B1's taxonomy. Also include the additional
items A1-A6 (confidence/calibration under shift, unlabeled shift detection via a domain
classifier, error-localization maps, a target-label-budget sweep K, full vs partial
fine-tuning, and metric-hygiene notes).

Produce jax-examples/notebooks/01_domain_shift_toy_v2.ipynb and leave the canonical
notebook untouched. Save no figures. Keep runtime modest on CPU. Verify it runs clean
end-to-end in the WinterSchool kernel and report ENHANCED GREEN with a summary of changes.
